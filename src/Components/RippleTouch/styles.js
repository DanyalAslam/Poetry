import { StyleSheet } from 'react-native'
import { vw } from '../../Units';

export default Styles = StyleSheet.create({

    imageStyle: {
        resizeMode: 'contain',
        width: 5 * vw,
        height: 5 * vw
    }
});