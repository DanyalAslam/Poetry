const INITIAL_STATE = {
    homeData: []
}


 const GeneralReducer = (state = INITIAL_STATE, action) => {

    switch (action.type) {

        default: {
            return {
                state
            }
        }
    }

}

export default GeneralReducer
